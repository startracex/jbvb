import Cata from "@/lib/layout/cata"
import Play from "@/lib/play/input-form/signform"

<Cata>

# sign-form

sign-form 包含一个基础的登录/注册表单

<Play></Play>





可用方法

`namevalue()` 获取name和value构成的数组
[name, value], 其中value为此元素的子元素的父表单所提供的FormData对象与内部全部元素namevalue()的两项数组合并成为对象的值合并后的对象

`FormData()` 获取此元素的子元素的父表单所提供的FormData对象与内部全部元素namevalue()的两项数组合并成为对象的值合并为一个FormData

</Cata>
