import Cata from "@/lib/layout/cata"
import BaseButton from "@/lib/play/items/basebutton"

<Cata>

# base-button

base-button 是基本的按钮组件,可以通过设置 `color` 属性来改变按钮的颜色,默认为black

设置`ghost=true`,更改背景,边框和文字颜色

去除默认颜色: `color="unset"`

它实际上是一个链接,可以设置 `href` 和 `target` 属性

<BaseButton/>



</Cata>
